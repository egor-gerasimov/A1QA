1. Метод getSize(). При нечетном размере контента размер файла округляется в меньшую сторону. Должен оставаться дробным.
2. Метод isExists(). Возвращает true в случае, когда нет файла с проверяемым именем, но есть файл, в части имени которого есть проверяемое имя.
3. Конструктор FileStorage(int size). Кажется, что availableSize при создании должен быть равен maxSize. Из-за этого не работает тест testWriteBySize1().
